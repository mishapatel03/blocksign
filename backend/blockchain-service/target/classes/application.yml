server:
  port: ${PORT:8084}

spring:
  application:
    name: blockchain-service
  datasource:
    url: jdbc:postgresql://dpg-d997fncs728c73crljn0-a.oregon-postgres.render.com:5432/blocksign_docs?sslmode=require
    username: blocksign_user
    password: 7KTbsnTKchrW2KcNoKGnFqg8d6Pj1kEn
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5
      minimum-idle: 1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

blockchain:
  infura:
    url: ${INFURA_URL:https://sepolia.infura.io/v3/YOUR_KEY}
  private-key: ${BLOCKCHAIN_PRIVATE_KEY:mock-key}
  network: sepolia
